<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.just.six.music">
	<select id="doSelectOne"
		parameterType="com.just.six.user.domain.MusicVO"
		resultType="com.just.six.user.domain.MusicVO">
		SELECT *
		FROM jl_info
		WHERE id = #{id}
	</select>
</mapper>